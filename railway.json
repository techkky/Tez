{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "npm install --prefix client --include=dev && npm install --prefix server && npm run build --prefix client"
  },
  "deploy": {
    "startCommand": "npm run start --prefix server"
  }
}
